{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Archivo Actual (Debug)",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "justMyCode": true,
            "cwd": "${workspaceFolder}",
            "env": {
                "DB_USER": "admin_test",
                "ENV": "development"
            }
        }
    ]
}
